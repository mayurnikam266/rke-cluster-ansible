
# tasks file for rke2_master_config


---
- name: Create RKE2 config directory
  file:
    path: /etc/rancher/rke2
    state: directory
    mode: "0755"

- name: Deploy master config.yaml
  template:
    src: config-master.yaml.j2
    dest: /etc/rancher/rke2/config.yaml
    mode: "0600"

